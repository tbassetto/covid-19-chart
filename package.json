{
  "private": true,
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "build": "cd app && yarn build && yarn export",
    "dev": "cd app && yarn dev",
    "lint": "yarn workspaces run lint",
    "prettier": "yarn workspaces run prettier"
  },
  "license": "MIT",
  "repository": "github:tbassetto/covid-19-chart",
  "author": "Thomas Bassetto",
  "engines": {
    "yarn": "^1.0.0"
  }
}
